services:

  frontend:
    build:
      dockerfile: Dockerfile
      context: ./frontend
    ports:
      - 8000:80
    networks:
      - local
      
networks:
  local:

volumes:
    pgvol:
        external: true